[build]
  command = "vite build"
  publish = "docs"

# SPA redirection: ensures refreshing the page or visiting sub-routes is handled correctly
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
